node_modules/
dist/
.astro/
.vercel/
.env
.env.*
!.env.example
.DS_Store
npm-debug.log*
.vercel
.env*
.env*.local

# Scraped ads (viven en Supabase storage, no en el repo)
public/scraped_ads/
public/scraped_videos/
scripts/_quality_review_cache.json
scripts/_curation_cache/
work/
__pycache__/
*.pyc

# Estado local del CLI de Supabase: expone project-ref y la URL del pooler.
supabase/.temp/

# Logs de las corridas de scraping
scripts/*.log
scripts/_*.log

# Credenciales de despliegue (token personal de Supabase). Nunca se commitea.
.env.deploy

# Comparaciones de calidad generadas a mano
comparacion-calidad/
matriz/
matriz-antes/
.claves-anteriores.json

# Scripts de un solo uso que se escriben durante una sesión de trabajo.
zz-*
